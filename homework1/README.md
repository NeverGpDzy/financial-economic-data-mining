# 《金融与经济数据挖掘》作业1：机器学习技术分析实战

学习课件，教材及参考资料，上网调研，使用PYTHON编程，完成以下工作：

## 一、作业要求

1. 研究标的：任选白酒行业1家上市公司（600519 贵州茅台、000858 五粮液、000568 泸州老窖、600809 山西汾酒、600236 洋河股份）

2. 数据要求
   - （1）数据源：推荐使用 baostock（免费无需权限）
   - （2）数据类型：日收盘价（前复权）、日成交量
   - （3）固定时间范围：2024-01-01 至 2025-12-31（共2年交易日数据）

3. 建模规则
   - （1）滚动窗口：分别计算日价格收益率（价格变动指标）、日成交量收益率（成交量变动指标）；构造过去 10 日价格变动序列 + 过去 10 日成交量变动序列作为滚动时序特征，预测未来 1 天价格收益率（未来价格变动）。

     计算公式：
     - 价格收益率 = (当日收盘价 − 前一日收盘价) / 前一日收盘价
     - 成交量收益率 = (当日成交量 − 前一日成交量) / 前一日成交量

   - （2）技术手段：使用LightGBM regressor（也可以自选任何自己想用的回归模型），并以线性回归模型或者随机森林回归模型作为基线模型对比效果；

4. 数据分析要求
   - （1）模型评估：使用 2024 年（前 12 个月）数据训练模型，按时间顺序划分训练集 / 测试集（前 80% 训练，后 20% 测试，仅用于模型评估）；
   - （2）实战测试：使用全部 2024 年数据训练最终模型；使用训练好的模型，对2025年（后12个月）数据量化回测，验证投资效果

5. 投资策略（统一标准）
   - （1）初始资金：100万元
   - （2）交易手续费：0.03%（买入、卖出各收取一次，双向计费）
   - （3）买卖规则：
     - ✅ 预测次日涨幅 > 0.5%：今日满仓买入（已持仓则不动）
     - ❌ 预测次日跌幅 > 0.5%：今日全部卖出（空仓则不动）

6. 输出要求
   - （1）输出各模型的机器学习指标：RMSE、MAE（模型拟合精度）；
   - （2）输出各模型的金融量化指标：累计收益、最大回撤、胜率（实盘价值），画出模型预测效果图，回测累计收益曲线
   - （3）结果解读与模型优缺点分析

## 二、敏感性分析

### 实验1：滑动窗口敏感性（10天 vs 20天）

在基准实验（窗口=10天）基础上，将滑动窗口改为20天，对比分析窗口大小对模型预测和回测表现的影响。

### 实验2：统计周期敏感性（不同训练-回测周期）

保持窗口=10天不变，改变训练和回测的时间区间：
- 2023年训练 → 2024年回测
- 2022年训练 → 2023年回测

与基准实验（2024训练→2025回测）对比，分析模型在不同市场环境下的稳定性。

### 运行方式

```bash
python homework1/sensitivity_analysis.py
```

结果输出至 `outputs/homework1/sensitivity/` 目录。

## 三、提交要求

1. 材料：PPT（步骤+结果+分析）+ Python源码（完整可运行，带注释）
2. 提交方式：学习通上传PPT附件 + 代码正文/附件
3. 考核：代码正确性 + 数据分析合理性 + 课堂展示

## 项目框架模板

```python
# 导入库
import baostock as bs
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from sklearn.ensemble import RandomForestRegressor
from lightgbm import LGBMRegressor
from sklearn.metrics import mean_squared_error, mean_absolute_error

# ======== 1. 固定参数========
stock_codes = ["sh.600519","sz.000858","sz.000568","sh.600809","sh.600236"]
start_date = "2024-01-01"
end_date = "2025-12-31"
window = 10   # 10天滚动窗口

# ======== 任务1：数据获取（自己补全核心代码）========
# 1. 登录baostock
# 2. 获取股票数据：日期、前复权收盘价(close)、成交量(volume)
# 3. 数据清洗：转换数值类型、设置日期索引
# 提示：fields = "date,close,volume"，adjustflag="3"（前复权）

# ======== 任务2：时序特征构建（核心！变动预测变动）========
# 1. 计算【价格收益率】(价格变动)
df['price_return'] = df['close'].pct_change()
# 2. 计算【成交量收益率】(成交量变动)
df['vol_return'] = df['volume'].pct_change()
# 3. 构建标签：次日价格收益率（预测目标）
df['label'] = df['price_return'].shift(-1)

# 4. 构造10天滚动特征（学生补全）
# 过去10天 价格收益率：price_return_1 ~ price_return_10
# 过去10天 成交量收益率：vol_return_1 ~ vol_return_10
for i in range(1, window+1):
    df[f'price_return_{i}'] = df['price_return'].shift(i)
    df[f'vol_return_{i}'] = df['vol_return'].shift(i)

# 5. 删除缺失值，划分特征X和标签y
df = df.dropna()
# 特征列：20个特征（10个价变动 + 10个量变动）
feature_cols = [f'price_return_{i}' for i in range(1, window+1)] + [f'vol_return_{i}' for i in range(1, window+1)]
X = df[feature_cols]
y = df['label']

# ======== 任务3：严格时序数据划分（禁止随机划分）========
# 1. 2024年数据：用于模型训练与评估（前80%训练，后20%测试）
train_val_mask = df.index < "2025-01-01"
# 2. 2025年数据：用于量化回测（最终模型用全部2024年数据训练）
test_mask = df.index >= "2025-01-01"

# ======== 任务4：三大模型训练 + 评估（RMSE、MAE）========
# 模型列表：线性回归、随机森林回归、LightGBM回归
# 1. 2024年数据拆分，评估模型性能
# 2. 用全部2024年数据训练最终模型
# 3. 输出测试集RMSE、MAE

# ======== 任务5：量化回测（作业统一规则）========
# 初始资金：100万
# 手续费：买入/卖出各0.03%（双向）
# 买卖规则：
# 预测次日涨幅 > 0.5% → 满仓买入
# 预测次日跌幅 > 0.5% → 全部卖出
# 计算指标：累计收益、最大回撤、预测胜率

# ======== 任务6：结果可视化========
# 1. 绘制模型预测值vs真实值对比图
# 2. 绘制2025年三大模型回测资金收益曲线
```
