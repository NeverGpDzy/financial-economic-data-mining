# 实验二 LaTeX 报告

本目录保存实验二标准实验报告的 LaTeX 源码，格式参考 `D:\A西南石油\大数据应用分析实践\Code\reports\final` 和本仓库 `report/experiment1_latex`。

编译命令：

```powershell
cd report/experiment2_latex
latexmk -xelatex -outdir=build main.tex
```

PDF 输出：

```text
report/experiment2_latex/build/main.pdf
```

