# 实验一 AI代码审查与修复表

| 编号 | 审查发现 | 影响 | 修复动作 | 验证 |
| --- | --- | --- | --- | --- |
| 1 | 指导书文件夹名为实验一，但正文包含实验一、二、三连续链路。 | 容易把实验一输出和后续建模输出混在一起。 | 报告中明确实验一主任务为周度情绪指标，同时保留完整链路输出供后续实验使用。 | `assignment.md` 和报告均说明三段任务关系。 |
| 2 | 沪深300价格文件扩展名为 `.xls`，实际为GBK编码制表符文本。 | 直接用Excel读取可能失败或字段错位。 | 使用 `pd.read_csv(..., sep=\"\\t\", encoding=\"gbk\", skiprows=2)` 读取并重命名字段。 | `hs300_weekly_return.csv` 成功生成。 |
| 3 | 新闻包含非交易日、空文本和重复正文。 | 周度情绪口径会混入无效样本，影响情绪比例。 | 先做日期范围、非空、去重，再按沪深300交易日过滤。 | `summary.json` 记录过滤前后行数。 |
| 4 | 情感模型文件可能在其他机器上不存在。 | 代码无法直接运行，影响提交可复现性。 | 优先使用本地FinBERT；缺失时自动退化为金融词典规则，并在报告说明标注方法。 | `sentiment.py` 中提供BERT和规则两套路径。 |
| 5 | 周度统计如果按自然周会与“5个交易日一周”要求不一致。 | 周度样本数量和后续羊群指标会偏离指导书。 | 使用交易日序号整除形成5日交易周。 | `weekly_sentiment.csv` 输出51个交易周样本。 |
| 6 | 只输出CSV不能体现数据库存储结果。 | 不满足“存入数据库并导出CSV”的要求。 | 使用SQLite写入 `weekly_sentiment`、`sentiment_labeled_sample` 等表。 | `experiment1.db` 随结果包提交。 |
